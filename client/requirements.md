## Packages
recharts | For manager dashboard analytics charts (Sales by Floor, Daily Sales)
framer-motion | For smooth page transitions, card animations, and modal effects
clsx | Utility for conditional class names (usually installed but good to ensure)
tailwind-merge | Utility for merging tailwind classes (usually installed but good to ensure)

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
AI Chatbot connects to /api/chat endpoint using SSE or fetch streaming patterns.
Images use Unsplash source URLs for realistic grocery placeholders.
